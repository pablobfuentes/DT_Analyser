[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

# SPA: send all routes to index.html so React Router handles them.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
