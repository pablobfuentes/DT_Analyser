# Render / Docker defaults (see render.yaml)
LTA_DATA_DIR=/var/data
LTA_DATABASE_URL=sqlite:////var/data/trader_analyzer.db
LTA_DISABLE_AUTOMATION=true
# Comma-separated Netlify (and custom) origins
LTA_CORS_ORIGINS=https://your-site.netlify.app
